#!/bin/sh
# Git Pre-Commit Hook - 调用 git-ai 工具
# 让原生 git 命令获得 git-ai 的功能
# 
# 安装方法：
#   cp .git-hooks/pre-commit-gitai .git/hooks/pre-commit
#   chmod +x .git/hooks/pre-commit

# 调用 git-ai 的 pre-commit 处理
# git-ai 会执行 checkpoint 操作来追踪 AI 代码
git-ai checkpoint --kind Human --quiet || exit 0

exit 0
